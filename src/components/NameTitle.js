import { Typography } from "@material-ui/core";

const NameTitle = () => {
  return <Typography style={styles.pageTitle}>RYLAN COLE</Typography>;
};

const styles = {
  pageTitle: {
    color: "black",
    fontSize: 64,
  },
};

export default NameTitle;
